[package]
name = "infrastructure"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
config.workspace = true
serde.workspace = true

domain = { path = "../domain" }
shared = { path = "../shared" }
